framework module OmniFileExchange {
    // Needed for the iOS framework, since Xcode defaults to the target's name, not the product's name.
    umbrella header "OmniFileExchange.h"

    export *
    module * { export * }
    
    explicit module Internal {
        header "OFXAccountAgent.h"
        header "OFXAccountAgent-Internal.h"
    }

}
